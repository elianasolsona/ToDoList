const toDoList = document.querySelector("#stuffList");

function showList(lists) {
    lists.forEach(list =>{
        const element = `<div class="list">
        <ul>
            <li></li>
        </ul></div>`
    })

}